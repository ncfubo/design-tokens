{
  "devDependencies": {
    "style-dictionary": "^3.7.0",
    "token-transformer": "^0.0.20"
  },
  "scripts": {
    "build": "token-transformer data/tokens.json data/output.json && style-dictionary build"
  }
}
